#asking the user for an x and y value, this will return a string
#x = input('What is x? ')
#y = input('what is y? ')

#creating a new vakue by adding the values provided by the user
#z = int(x) + int(y)

#printing return value z from above code 
#print(f'{z}')

#alternatively we can nest the input fuction into an int function so that the return value becomes the argument for the new function.

#x = int(input('what is x? '))
#y = int(input('what is y? '))

#print(x + y)

#x = float(input('what is x? '))
#y = float(input('what is y? '))

#z = round(x + y)

#print(f'{z:,}')
